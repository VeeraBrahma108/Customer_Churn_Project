import pandas as pd
import numpy as np
import os

# --- adjust the path if your notebook isn't in Customer_Churn_Project/notebooks ---


# create folder if missing
os.makedirs(os.path.dirname(out_path), exist_ok=True)

# build synthetic dataset
n = 300
rng = np.random.default_rng(42)
df = pd.DataFrame({
    "customer_id": np.arange(1, n+1),
    "purchase_frequency": rng.integers(1, 30, size=n),   # purchases per period
    "spending": np.round(rng.normal(500, 200, size=n).clip(20), 2),  # spending
    "engagement": rng.integers(1, 100, size=n),  # engagement score
    "last_purchase_days": rng.integers(0, 600, size=n)  # days since last purchase
})

df.to_csv(out_path, index=False)
print("Saved synthetic file to:", out_path)
print("First 5 rows:")
print(df.head())
